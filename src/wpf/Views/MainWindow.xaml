<materialExtension:MaterialWindow x:Class="Mitheti.Wpf.Views.MainWindow" xml:lang="en-US"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                     xmlns:materialExtension="clr-namespace:MaterialDesignExtensions.Controls;assembly=MaterialDesignExtensions"
                     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
                     TextElement.FontWeight="Regular"
                     TextElement.FontSize="12"
                     TextOptions.TextFormattingMode="Ideal"
                     TextOptions.TextRenderingMode="Auto"
                     Background="{DynamicResource MaterialDesignPaper}"
                     FontFamily="{DynamicResource MaterialDesignFont}"
                     mc:Ignorable="d"
                     Height="500" Width="700" ResizeMode="NoResize" Icon="/Resources/app.png">
    <Grid>
        <!-- TODO: add settings changing; -->
        <!-- TODO: add database management (clear, see size of it); -->
        <TabControl x:Name="TabControl" Style="{StaticResource MaterialDesignTabControl}" />
    </Grid>
</materialExtension:MaterialWindow>