<UserControl x:Class="Mitheti.Wpf.Views.MainTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Loaded="OnLoaded"
             d:DesignHeight="500" d:DesignWidth="350">
    <StackPanel>
        <Label Content="{Binding IsLaunchedString}" />
        <Button x:Name="LauncherButton" Click="OnLauncherClick" />
        <Button Click="OnExitClick" Content="{Binding Localization[Window:Main:Button:Exit]}" />
    </StackPanel>
</UserControl>